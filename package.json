{
  "name": "eas-4-tbsync",
  "version": "5.0.13",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev",
    "version": "node build.js && git add src",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
