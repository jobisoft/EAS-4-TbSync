// Artificial delay the provider inserts after each reportSyncState /
// reportProgress notification so the manager UI has time to paint each
// phase. Development aid - set to 0 to disable pacing entirely.
export const DEBUG_STATUS_DELAY_MS = 0;
